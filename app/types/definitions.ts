export type TodoType = {
  userId: number
  id: string
  title: string
  completed: boolean
};

export type UserType = {
  id: number
  username: string
  role: string
  skill: string
};
